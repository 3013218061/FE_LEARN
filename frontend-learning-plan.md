# Java 后端程序员前端系统学习计划

## 1. 背景与目标

本文面向具备数年 Java 后端开发经验、但前端基础较弱的中国互联网开发者。学习目标不是短期内成为纯前端专家，而是快速建立现代前端开发的完整认知，并能够使用 Codex 高效完成真实项目开发。

阶段性目标：

- 看懂中等复杂度的 React + TypeScript 项目结构。
- 独立开发后台管理类页面。
- 能与 Spring Boot REST API 完成联调。
- 能使用 Codex 拆解需求、生成代码、审查代码、补充测试。
- 遇到问题时能从浏览器渲染、网络请求、状态管理、构建工具等层面定位问题。

## 2. 技术路线

推荐路线：

```text
HTML/CSS/JavaScript
  -> TypeScript
  -> React
  -> Vite
  -> 接口联调
  -> 测试与部署
  -> Codex 辅助项目开发
```

选型理由：

- HTML/CSS/JavaScript 是浏览器原生基础，必须先理解页面结构、样式和交互。
- TypeScript 能显著降低前端大型项目维护成本，也更接近 Java 开发者的类型思维。
- React 是当前主流前端框架之一，生态成熟，适合从组件化和状态驱动 UI 入门。
- Vite 启动快、配置轻，适合作为现代前端工程化入门工具。
- 后台管理类项目与 Java 后端业务场景贴近，学习投入能快速转化为项目能力。

## 3. 第一周：Web 基础

核心目标：理解浏览器页面的基本组成，不追求面面俱到，但要求能独立写出静态页面。

学习重点：

- HTML：页面结构、语义化标签、表单、表格。
- CSS：选择器、盒模型、Flex、Grid、定位、响应式。
- JavaScript：变量、函数、数组、对象、事件、Promise、async/await、fetch。
- 浏览器：DOM、事件机制、开发者工具、Network 面板。

建议产出：

- 实现一个静态后台首页。
- 页面包含左侧菜单、顶部导航、数据卡片、筛选区域、数据表格。
- 使用浏览器开发者工具检查布局、样式和网络请求。

与 Java 后端类比：

- HTML 类似数据结构声明，描述页面有哪些内容。
- CSS 类似展示层规则，控制内容如何排布和呈现。
- JavaScript 类似运行在浏览器中的业务逻辑层，但更强调事件驱动和异步执行。

## 4. 第二周：TypeScript 与工程化

核心目标：建立前端项目工程化意识，理解类型、依赖、构建和项目结构。

学习重点：

- TypeScript 基础类型：string、number、boolean、数组、对象。
- interface、type、联合类型、泛型。
- npm、package.json、依赖版本、脚本命令。
- Vite 项目结构、开发服务器、构建产物。
- 环境变量与不同环境配置。

建议产出：

- 创建一个 Vite + React + TypeScript 项目。
- 定义后端接口返回类型。
- 封装基础 API 请求方法。

示例类型：

```ts
interface UserVO {
  id: number;
  name: string;
  role: string;
  status: 'enabled' | 'disabled';
}
```

## 5. 第三周：React 核心

核心目标：理解 React 的状态驱动 UI 模型。

学习重点：

- JSX。
- 函数组件。
- props。
- useState。
- useEffect。
- 条件渲染。
- 列表渲染。
- 表单受控组件。
- 父子组件通信。
- loading、empty、error 状态处理。

关键心智模型：

```text
UI = f(state)
```

页面不是手动改 DOM，而是通过状态变化触发重新渲染。对 Java 后端开发者来说，可以把 React 组件理解为“带界面的函数”，props 类似方法入参，state 类似组件内部状态。

建议产出：

- 实现用户管理页面。
- 支持查询用户列表、新增用户、编辑用户、启用/禁用用户。
- 补齐表单校验、加载状态、空数据状态、异常提示。

## 6. 第四周：真实项目开发能力

核心目标：具备完成一个小型后台管理模块的能力。

学习重点：

- 路由管理。
- API 分层封装。
- token 鉴权。
- 请求拦截与统一错误处理。
- 分页、筛选、排序。
- 表单抽象。
- 前后端接口联调。
- 构建与部署。

推荐后续引入：

- TanStack Query：用于管理服务端状态、缓存、刷新和请求状态。
- React Router：用于前端路由。
- Vitest：用于单元测试。
- Playwright：用于端到端测试。

技术权衡：

- 初学阶段不建议过早引入复杂全局状态管理。大多数后台页面先区分清楚“服务端状态”和“本地 UI 状态”即可。
- 组件库可以提高效率，但不要让组件库掩盖 HTML、CSS、React 状态模型这些基础。
- 对后台管理系统而言，稳定性、可维护性和交互一致性优先于视觉炫技。

## 7. Codex 辅助开发工作流

推荐使用方式：

1. 让 Codex 解释现有项目结构。
2. 让 Codex 给出组件拆分和数据流方案。
3. 先生成 TypeScript 类型定义和 API 层。
4. 再实现页面组件。
5. 要求 Codex 自查 loading、empty、error、权限、表单校验等边界状态。
6. 要求 Codex 运行构建、测试和代码检查。
7. 最后让 Codex 按代码审查视角检查可维护性问题。

常用提示词：

```text
我有 Java 后端经验，但前端不熟。请先解释这个 React + TypeScript 文件的职责、数据流、状态变化和组件关系，不要直接改代码。
```

```text
请基于现有项目风格，实现一个用户管理页面。先给出设计思路和组件拆分，确认后再修改代码。
```

```text
请审查这个前端实现，重点看类型设计、状态管理、接口错误处理、表单校验和可维护性。
```

## 8. 阶段验收清单

完成本计划后，应能回答并实践以下问题：

- HTML、CSS、JavaScript 分别解决什么问题？
- Flex 和 Grid 适合哪些布局场景？
- Promise 和 async/await 的关系是什么？
- TypeScript interface 和 Java DTO 有什么相似点和差异？
- React props 和 state 分别适合表达什么？
- useEffect 适合处理哪些副作用？
- 如何封装一个可维护的 API 请求层？
- 如何处理接口 loading、empty、error 三种状态？
- 如何与 Spring Boot 接口联调？
- 如何让 Codex 帮你完成前端需求拆解、实现、测试和审查？

## 9. 后续进阶路线

当能够独立完成后台管理模块后，可以继续学习：

- 前端性能优化：首屏加载、代码分包、缓存、渲染性能。
- 组件设计：可复用组件、受控与非受控、组合模式。
- 权限系统：菜单权限、按钮权限、路由守卫。
- 前端架构：模块边界、领域建模、状态分层、错误处理规范。
- 质量保障：单元测试、集成测试、端到端测试、可访问性检查。
- 全栈协作：接口契约、OpenAPI、Mock、联调环境治理。

## 10. 推荐学习节奏

每天建议投入 1 到 2 小时：

- 30 分钟阅读或听讲。
- 60 分钟动手编码。
- 30 分钟让 Codex 解释、重构或审查当天代码。

不要只看教程。每个知识点都要落到一个小页面或小功能上，否则很难形成工程直觉。
