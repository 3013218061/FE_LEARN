# 开发环境配置（npm run dev 时生效）
# 对应 Spring Boot 的 application-dev.yml
# 用相对路径 /api：浏览器请求同源的 /api/*，由 vite.config.ts 的 server.proxy
# 转发到本地 Spring Boot（localhost:8080），同源通信绕开 CORS。
VITE_API_BASE_URL=/api
VITE_APP_TITLE=用户管理（开发环境）
# 是否启用 MSW mock。后端真实就绪后改为 false，请求会经 Vite 代理打到真后端，业务代码无需改动。
# 默认 true：前端可独立开发、E2E 也走 mock。需真后端联调时改 false 并启动 backend/（见 backend/README.md）。
VITE_USE_MOCK=true
